[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.9.0" # Adjust based on your Node version

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200